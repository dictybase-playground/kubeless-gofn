apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    kubernetes.io/ingress.class: nginx
  labels:
    app: kubeless-dashfn
  name: dashfn
  namespace: dictybase
spec:
  rules:
  - host: betafunc.dictybase.local
    http:
      paths:
      - backend:
          serviceName: dashfn
          servicePort: 8080
        path: /dashboard
  tls:
  - hosts:
    - betafunc.dictybase.local
    secretName: dictybeta-staging-org-tls
